{
  "name": "mern-app",
  "private": true,
  "scripts": {
    "frontend": "cd frontend && nodemon dev",
    "backend": "cd backend && nodemon dev",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "dependencies": {
    "nodemon": "^3.1.10"
  }
}
